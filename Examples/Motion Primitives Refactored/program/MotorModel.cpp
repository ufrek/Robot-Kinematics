#include "MotorModel.h"

//Bulds a Gaussian Mixture model from provided Parameters
MotorModel::MotorModel(std::vector<double> w, std::vector<double> c, std::vector<double>sdev) : weights(w), centers(c), stdev(sdev)
{
	//TODO: build a gaussian model from parameters.
}
